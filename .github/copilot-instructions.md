# GitHub Copilot Custom Commands

Ce fichier configure le système de slash commands personnalisées pour GitHub Copilot.

## 🎯 Système de Dispatch — INSTRUCTION OBLIGATOIRE

**Quand l'utilisateur tape `/command`, tu DOIS :**

1. **LIRE IMMÉDIATEMENT** le fichier `.github/github-copilot-cmd/command.md` (utilise `read_file`)
2. **EXÉCUTER** les instructions contenues dans ce fichier comme si c'était ton prompt principal
3. **RESPECTER** les `allowed-tools`, `validation`, `rules` définis dans ce fichier

> ⚠️ **CRITIQUE** : Ne réponds JAMAIS à une commande `/xxx` sans avoir d'abord lu le fichier `xxx.md` correspondant !

### Syntaxe

```
/command $ARGUMENTS
```

- `command` → nom du fichier `.md` à charger (sans extension)
- `$ARGUMENTS` → paramètres optionnels passés à la commande

---

## 📚 Commandes Disponibles

| Commande      | Description                                                | Fichier         |
| ------------- | ---------------------------------------------------------- | --------------- |
| `/create-cmd` | Génère une nouvelle commande personnalisée (mode itératif) | `create-cmd.md` |
| `/commit`     | Analyse le code et génère un message de commit uniforme    | `commit.md`     |
| `/doc`        | Agrège les CHANGENOTES en CHANGELOG versionné              | `doc.md`        |
| `/setup`      | Configure les settings VS Code pour Copilot                | `setup.md`      |

---

## 🔧 Ressources

- **Best Practices** : `.github/github-copilot-cmd/best-practices.md` — Catalogue des bonnes pratiques par domaine
- **Exemples** : `.github/github-copilot-cmd/examples/` — Commandes de référence gold-standard

---

## ⚡ Règles Globales

Toutes les commandes doivent respecter ces principes :

1. **Validation des prérequis** — Vérifier les conditions avant exécution
2. **Pas d'interactif** — Aucune commande demandant une saisie utilisateur
3. **Exit silencieux** — Si rien à faire, terminer sans message
4. **Pas de verbose** — Messages concis, pas de détails inutiles
5. **Pas de signatures** — Jamais de "Generated by..." ou équivalent
6. **Gestion des erreurs** — Reporter les erreurs clairement, sans verbosité
7. **Mode --help** — Si `$ARGUMENTS` contient `--help` ou `-h`, afficher l'aide de la commande

### Mode Help (`--help` / `-h`)

Quand l'utilisateur tape `/command --help`, afficher un résumé concis :

```
/<command> — <description courte>

Usage:
  /command [options] [arguments]

Options:
  <liste des arguments disponibles>

Examples:
  /command                  → comportement par défaut
  /command --option         → avec option
```

> ⚠️ **IMPORTANT** : En mode help, NE PAS exécuter la commande, juste afficher l'aide.

---

## 🚀 Création de Nouvelles Commandes

Pour créer une nouvelle commande, utiliser :

```
/create-cmd <nom-commande>
```

Le générateur `/create-cmd` :

- Analyse le sujet demandé
- Propose un draft complet (Description, Allowed Tools, Validation, Workflow, Rules)
- Itère avec l'utilisateur pour améliorer
- Crée le fichier `.github/github-copilot-cmd/<nom>.md`
- Propose d'enrichir `best-practices.md` avec les nouveaux patterns découverts
